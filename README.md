# ScapeGoat-Tree
Реализация Scapegoat Tree — самобалансирующегося бинарного дерева поиска на языке C.
Дерево поддерживает динамические операции вставки, удаления и поиска, обеспечивая амортизированную логарифмическую сложность за счёт периодической перебалансировки.

---

## Task Individual  

### Поддерживаемые типы данных  
• ключ — беззнаковое целое число;  
• значение — беззнаковое целое число.

## Тип дерева и содержимое узла  
Scapegoat-дерево (взвешенное дерево).  
Узел дерева должен содержать:  
• ключ;  
• указатели на левое и правое поддеревья;  
• указатели на родительский узел;  
• указатель на информационное поле;  
• опционально: дополнительные поля, необходимые для поддержания балансировки.  

### Принцип работы с дублирующимися ключами
В дереве могут храниться записи с дублирующимися ключами. Элементы с дублирующимися ключами должны храниться непосредственно в дереве или списке элементов, в соответствии с формулировкой задания.  
### Операции, поддерживаемые деревом
#### 1. Добавление нового элемента
Добавление нового элемента в дерево без нарушения свойств упорядоченности. Если запись с
данным ключом уже присутствует в дереве, а дублирование ключей не допускается, то необходимо вернуть ошибку.  
#### 2. Удаление элемента
Удаление элемента, заданного своим ключом, без нарушения свойств упорядоченности дерева.  
Если в дереве присутствуют несколько элементов с указанным ключом, то необходимо указать номер удаляемого элемента.  
#### 3. Обход дерева
Вывод содержимого дерева в прямом порядке следования ключей, превышающих заданный (если
ключ не указан — вывод содержимого всего дерева).  
#### 4. Поиск элемента по ключу
Поиск информации по заданному ключу. Если элементов с требуемым значением ключа может
быть несколько, то необходимо в качестве результата вернуть их все. Возврат необходимо осуществлять при помощи вектора или списка указателей, возврат копий элементов не допускается.  
#### 5. Специальный поиск элемента
Поиск элемента с ключом, значение которого наиболее близко к заданному, но не совпадает с ним (если таких элементов несколько — действовать по аналогии с операцией поиска по ключу).  

---

### Build with

```sh
make
```

### Launch

```sh
./lab
```

---

## Additional task
Особенности индивидуального задания: ключ — число, значение — число, дублирование ключей — разрешено.  
Формулировка: быстрый поиск числа в текстовом файле, строки которого содержат ноль или более чисел, разделённых запятой.  
Возвращаемое значение: вектор, состоящий из номеров строк, в которых присутствует искомое число.  
Switch directory to extra_task

### Build with

```sh
make
```

### Launch

```sh
./extra input_file_name
```
